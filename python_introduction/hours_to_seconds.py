# Define the variable 'hours' and assign the value
hours = 2

# Calculate the total seconds
seconds = hours * 3600

# Print the result in the required format
print(f"{hours} hour(s) is {seconds} seconds.")
